#ifndef EMPLEADO_H
#define EMPLEADO_H
#include <QString>

class Empleado
{
public:

    // Empleado(QString nombreEmpleado, int edad, float salario)
    //     : nombre(nombre), edad(edad), salario(salario) {}

    // virtual ~Empleado() {}
    // virtual float calcularSalario() = 0;
    // QString getNombre() const { return nombre; }
    // int getEdad() const { return edad; }
    // float getSalario() const { return salario; }

    // QString nombreEmpleado;
    // int edadEmpleado;
    // float salario;

};

#endif // EMPLEADO_H
